FROM node:18.18.0
ARG MONGODB_URI
ARG SECRET
ENV PORT=3001
# ENV NODE_ENV=prod
ENV MONGODB_URI=$MONGODB_URI
ENV SECRET=$SECRET
COPY dist/ /app
WORKDIR /app

EXPOSE 3001

CMD ["node", "app.bundle.js"]